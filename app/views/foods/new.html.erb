<h1>New Food</h1>
<%= form_with(model: @food) do |form| %>
  <% if @food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@food.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% @food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
   <%= form.label :name, style: "display: block"%> 
   <%= form.text_field :name, style: "display: block"%>
  </div>

  <div>
   <%= form.label :measurement_unit, style: "display: block"%> 
   <%= form.text_field :measurement_unit, style: "display: block"%>
  </div>

  <div>
   <%= form.label :price, style: "display: block"%> 
   <%= form.text_field :price, style: "display: block"%>
  </div>

 

  <div>
    <%= form.submit %>
  </div>
<% end %>
